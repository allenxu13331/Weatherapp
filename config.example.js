/**
 * Atmos — local configuration (EXAMPLE)
 *
 * Copy this file to `config.js`. `config.js` is gitignored.
 *
 * OpenWeather (optional but recommended for Today tab):
 *   Only the /weather “current conditions” call is used — real‑time temp, feels
 *   like, humidity, wind, pressure, visibility. Without a key, the Today hero
 *   falls back to Open‑Meteo’s current_weather + hourly estimates.
 *
 * Open‑Meteo (no key): geocoding, historical archive (past), and daily
 * forecast up to 16 days — used for the 5‑day strip, Forecast &amp; History,
 * and Weather Assistant.
 *
 * https://openweathermap.org/api
 */
window.OW_KEY = 'PASTE_OPENWEATHER_API_KEY_HERE';
